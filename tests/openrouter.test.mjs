import test from 'node:test';
import assert from 'node:assert/strict';
import {normalizeOpenRouterModel} from '../server/openrouter-catalogue.mjs';

test('OpenRouter catalogue metadata becomes priced capability-aware Routers models',()=>{const model=normalizeOpenRouterModel({id:'openai/example',name:'Example',context_length:128000,pricing:{prompt:'0.000001',completion:'0.000003',input_cache_read:'0.0000005'},architecture:{input_modalities:['text','image'],output_modalities:['text']},supported_parameters:['tools','response_format'],top_provider:{max_completion_tokens:8192}});const {inputRate,outputRate,cachedRate,reasoningRate,officialInput,officialOutput,...metadata}=model;assert.deepEqual(metadata,{id:'openai/example',name:'Example',family:'OpenAI',upstream:'openai/example',ratio:'3',context:128000,output:8192,vision:true,tools:true,json:true,premium:true,quality:81});assert.equal(inputRate,'2.2');assert.equal(outputRate,'6.6');assert.equal(cachedRate,'1.1');assert.equal(reasoningRate,'6.6');assert.equal(officialInput,'1');assert.equal(officialOutput,'3');});
test('OpenRouter catalogue excludes non-chat and unpriced models',()=>{assert.equal(normalizeOpenRouterModel({id:'vendor/image-model',context_length:10000,pricing:{prompt:'0',completion:'0'}}),null);assert.equal(normalizeOpenRouterModel({id:'vendor/chat',context_length:10000,pricing:{prompt:'bad',completion:'0'}}),null);});
